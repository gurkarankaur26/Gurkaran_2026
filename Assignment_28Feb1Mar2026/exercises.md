# Implement your own sorting algorithm.

#  Write a function to merge two lists that are already sorted. Then write a function to perform Merge Sort

# You have a list containing 0s and 1s. Propose various methods to sort them and report the time and space complexity for each.

# Binary Search Tree - left < num, right >= num.
 https://www.cs.usfca.edu/~galles/visualization/BST.html

Create node that has num, left and right. It should have two methods 
    - node.print(), it prints the tree in increasing order
    - node.insert(num), it should insert the num at the correct location.
    - node.search(num), print True if num exists

> sort([0, 1, 0, 0, 1, 0, 1])
[0, 0, 0, 0, 1, 1, 1]

# What is the order of complexity of 
    - multiplcation of matrics n x n with n x n?
    - multiplcation of matrics a x b with b x c?
    - solving n variable equation?
    - find a number in a unsorted list?
    - a binary search tree?
    - Your own sorting algorithms
    - check method of our decision tree?